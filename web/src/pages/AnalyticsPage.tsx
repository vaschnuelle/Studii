/**
 * Analytics: totals, streak, alignment (Phase 1 UI placeholder).
 */
export function AnalyticsPage() {
  return (
    <section>
      <h1>Analytics</h1>
      <p>
        Daily/weekly/monthly totals, streaks, top subjects, and habit alignment will consume cached
        fields and scheduled aggregation jobs.
      </p>
    </section>
  );
}
