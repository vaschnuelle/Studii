/**
 * Focus Timer + Pomodoro session controls (Phase 1 UI placeholder).
 */
export function FocusPage() {
  return (
    <section>
      <h1>Focus Timer</h1>
      <p>
        Session start/stop, Pomodoro cycles, and editable session notes will live here. Timers use
        local state first, then sync sessions on start/stop per PRD.
      </p>
    </section>
  );
}
