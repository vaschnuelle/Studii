/**
 * Friends: search, requests, live status (Phase 1 UI placeholder).
 */
export function FriendsPage() {
  return (
    <section>
      <h1>Friends</h1>
      <p>
        Search, friend requests via <code>createFriendRelation</code> /{' '}
        <code>respondFriendRequest</code>, and live status will be wired next.
      </p>
    </section>
  );
}
