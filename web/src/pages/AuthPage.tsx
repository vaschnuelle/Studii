/**
 * Sign-in placeholder — replace with email + Google flows using Firebase Auth.
 */
export function AuthPage() {
  return (
    <section>
      <h1>Sign in</h1>
      <p>Email/password and Google sign-in UI will be added alongside protected routes.</p>
    </section>
  );
}
